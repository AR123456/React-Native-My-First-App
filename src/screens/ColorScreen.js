import React from "react";
import { View, Button, StyleSheet } from "react-native";

const ColorScreen = () => {
  return <Button title="Add a Color">Color Screen</Button>;
};
const styles = StyleSheet.create({});
export default ColorScreen;
