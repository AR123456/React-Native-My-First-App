import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ShowScreen = () => {
  return (
    <View>
      <Text>Blog Post show screen </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ShowScreen;
